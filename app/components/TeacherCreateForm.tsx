"use client";import{useState,type FormEvent}from"react";import{Button,Input,Select}from"./ui";export function TeacherCreateForm(){const[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/admin/teachers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),email:f.get("email"),role:f.get("role")})});setMsg(r.ok?"교직원을 등록했습니다.":"등록할 수 없습니다. 이메일을 확인해주세요.");if(r.ok)location.reload()}return <form onSubmit={submit} className="grid gap-3 sm:grid-cols-4"><Input label="이름" name="name" required/><Input label="이메일" name="email" type="email" required/><Select label="역할" name="role"><option value="teacher">교사</option><option value="admin">관리자</option></Select><Button className="self-end">교사 추가</Button>{msg&&<p className="text-sm text-slate-600 sm:col-span-4">{msg}</p>}</form>}

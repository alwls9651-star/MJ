import{NextResponse}from"next/server";import{getCurrentStaff}from"../../../lib/staff-auth";import{getSupabaseServerClient}from"../../../lib/supabase-server";import{parseCourseInput}from"../../../lib/course-validation";
export async function POST(request:Request){try{const staff=await getCurrentStaff();if(!staff)return NextResponse.json({message:"로그인이 필요합니다."},{status:401});if(staff.role!=="admin")return NextResponse.json({message:"관리자 권한이 필요합니다."},{status:403});const input=parseCourseInput(await request.json());if(!input)return NextResponse.json({message:"입력값을 다시 확인해주세요."},{status:400});const service=getSupabaseServerClient(),{data:teacher}=await service.from("teachers").select("id").eq("id",input.teacher_id).eq("is_active",true).in("role",["teacher","admin"]).maybeSingle();if(!teacher)return NextResponse.json({message:"활성 상태인 담당 교사를 선택해주세요."},{status:400});const{error}=await service.from("courses").insert(input);if(error?.code==="23505")return NextResponse.json({message:"같은 연번의 강좌가 이미 존재합니다."},{status:409});if(error)throw error;return NextResponse.json({ok:true},{status:201})}catch{return NextResponse.json({message:"강좌를 저장하는 중 문제가 발생했습니다."},{status:500})}}

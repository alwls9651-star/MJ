import{NextResponse}from"next/server";import{requireStaff}from"../../../lib/staff-auth";import{getSupabaseServerClient}from"../../../lib/supabase-server";export async function POST(request:Request){try{await requireStaff(["admin"]);const b=await request.json()as Record<string,unknown>,name=typeof b.name==="string"?b.name.trim():"",email=typeof b.email==="string"?b.email.trim().toLowerCase():"",role=b.role==="admin"?"admin":"teacher";if(!name||!email.includes("@"))return NextResponse.json({error:"invalid"},{status:400});const{error}=await getSupabaseServerClient().from("teachers").insert({name,email,role});if(error)return NextResponse.json({error:"failed"},{status:400});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"unauthorized"},{status:401})}}

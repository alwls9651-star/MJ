import{NextResponse}from"next/server";import{requireStaff}from"../../../../../lib/staff-auth";import{getSupabaseServerClient}from"../../../../../lib/supabase-server";export async function POST(request:Request,{params}:{params:Promise<{teacherId:string}>}){try{const admin=await requireStaff(["admin"]),{teacherId}=await params,b=await request.json()as{isActive?:unknown};if(teacherId===admin.id||typeof b.isActive!=="boolean")return NextResponse.json({error:"invalid"},{status:400});const{error}=await getSupabaseServerClient().from("teachers").update({is_active:b.isActive}).eq("id",teacherId);if(error)return NextResponse.json({error:"failed"},{status:400});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"unauthorized"},{status:401})}}

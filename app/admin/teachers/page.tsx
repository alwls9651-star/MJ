import{AppShell,Badge,Card,PageHeader,Table}from"../../components/ui";import{TeacherCreateForm}from"../../components/TeacherCreateForm";import{TeacherStatusButton}from"../../components/TeacherStatusButton";import{requireStaff}from"../../lib/staff-auth";import{getSupabaseServerClient}from"../../lib/supabase-server";import type{Teacher}from"../../types/database";export const dynamic="force-dynamic";export default async function Page(){const admin=await requireStaff(["admin"]);const result=await getSupabaseServerClient().from("teachers").select("id,name,email,role,is_active").order("name").returns<Array<Pick<Teacher,"id"|"name"|"email"|"role"|"is_active">>>();const teachers=result.data??[];return <AppShell role="admin"><PageHeader title="교사 관리" description="로그인 가능한 교직원 whitelist를 관리합니다."/><Card className="mb-5 p-5"><TeacherCreateForm/></Card><Card><Table headers={["이름","이메일","역할","상태","관리"]}>{teachers.map(t=><tr key={t.id}><td>{t.name}</td><td>{t.email??"미등록"}</td><td><Badge>{t.role}</Badge></td><td><Badge tone={t.is_active?"mint":"gray"}>{t.is_active?"활성":"비활성"}</Badge></td><td>{t.id===admin.id?<span className="text-xs text-slate-400">현재 계정</span>:<TeacherStatusButton id={t.id} active={t.is_active}/>}</td></tr>)}</Table></Card></AppShell>}

import{redirect}from"next/navigation";import{AppShell,Card,PageHeader}from"../../components/ui";import{ChangeStudentPasswordForm}from"../../components/ChangeStudentPasswordForm";import{getStudentSession}from"../../lib/student-session";import{getSupabaseServerClient}from"../../lib/supabase-server";import type{Student}from"../../types/database";export const dynamic="force-dynamic";export default async function Page(){const session=await getStudentSession();if(!session)redirect("/");const{data}=await getSupabaseServerClient().from("students").select("password_updated_at,must_change_password").eq("id",session.studentId).returns<Array<Pick<Student,"password_updated_at"|"must_change_password">>>().maybeSingle();if(!data?.password_updated_at)redirect("/");if(!data.must_change_password)redirect("/courses");return <AppShell role="student"><div className="mx-auto max-w-lg"><PageHeader title="새 비밀번호를 설정해주세요" description="임시 비밀번호로 로그인했습니다. 안전한 사용을 위해 변경해주세요."/><Card className="p-6"><ChangeStudentPasswordForm firstUse={true}/></Card></div></AppShell>}
